name: personality_quiz_app
description:  A personality quiz app with clean architecture
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk:  '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  
  # Dependency Injection
  get_it: ^7.6.4
  
  # Functional Programming
  dartz: ^0.10.1
  
  # Date & Time
  intl: ^0.18.1
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # HTTP & AI Integration
  http: ^1.1.0
  
  # UI
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: 
  uses-material-design:  true